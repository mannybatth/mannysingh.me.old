.col-md-6
    %li.portfolio-item
        %figure
            %img.img-portfolio.img-responsive{:alt => project.title, :src => "/images/screenshots/"+project.screenshots.split(',').first}/
            %figcaption
                %a{:href => "/projects/show/"+project.id.to_s}
                    %h3= project.title
                / %span caption here
                =link_to "Project details", url_for(:projects, :show, :id => project.id), :class => "link_to_btn"
