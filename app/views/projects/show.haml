- @title = @project.title
#show
  .project
    .title= @project.title
    .date= time_ago_in_words(@project.created_at || Time.now) + ' ago'
    .body= simple_format(@project.body.to_s.html_safe)
%p= link_to 'View all projects', url_for(:projects, :index)
